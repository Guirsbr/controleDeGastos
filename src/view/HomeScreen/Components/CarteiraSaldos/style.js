import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    boxCarteiraSaldos: {
        marginTop:20,
        alignItems:"center",
    },
    textCarteiraSaldos: {
        fontSize:18,
        color:"#000000",
        padding:10,
    },
});

export default styles