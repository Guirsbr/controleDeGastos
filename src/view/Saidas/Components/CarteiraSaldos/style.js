import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    boxCarteiraSaldos: {
        alignItems:"center",
    },
    textCarteiraSaldos: {
        fontSize:18,
        paddingBottom:10,
    },
});

export default styles